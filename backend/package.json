{
  "name": "photo-archive-manager-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "type-check": "tsc --project tsconfig.json --noEmit",
    "test": "vitest",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.5",
    "@types/supertest": "^2.0.12",
    "supertest": "^6.3.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.1.6",
    "vitest": "^1.6.0"
  }
}
